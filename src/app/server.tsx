// Removed legacy ai/rsc + LangChain streaming helper.
// Streaming is now handled via the API route at `src/app/api/chat/route.ts`
// using AI SDK v5 `streamText` and consumed on the client via `useChat`
// or a small streaming fetch utility.
